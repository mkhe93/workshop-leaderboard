## Leaderboard

- Ask the attendees to clone repo (or login in one of the few setup machines)
- Obtain the admin api key via secrets.devboost.com
- Run the get-api-key.sh script using FirstName LastName ApiKey arguments
- Start a new shell session
- 